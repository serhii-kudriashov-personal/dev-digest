/** L07 — digests literals. */

/** How many convention candidates a digest is allowed to quote. */
export const DIGEST_SUMMARY_LIMIT = 10;

/** Job kind for the weekly digest build. */
export const DIGEST_BUILD_JOB_KIND = 'digest.build';
