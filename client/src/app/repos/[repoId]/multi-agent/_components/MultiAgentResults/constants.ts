/** Constants for MultiAgentResults. */

/** Minimum column width for the Columns-mode grid — below this, a lane
 *  wraps to the next row rather than being squeezed unreadably narrow. */
export const LANE_MIN_WIDTH = 300;
