{
  "name": "@devdigest/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "arch": "depcruise src ../reviewer-core/src --config .dependency-cruiser.cjs",
    "test": "vitest run",
    "verify:l03": "pnpm typecheck && pnpm arch && pnpm verify:docker && vitest run intent --no-file-parallelism",
    "verify:l06": "pnpm typecheck && pnpm arch && pnpm verify:docker && vitest run eval --no-file-parallelism",
    "verify:docker": "docker info > /dev/null 2>&1 || (echo 'FAIL: no reachable Docker daemon. intent.it.test.ts gates on dockerAvailable() and would SKIP — a skipped lane is unverified, not green (specs/l03-intent-layer.md, Verification plan).' && exit 1)",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx src/db/migrate.ts",
    "db:seed": "tsx src/db/seed.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.33.1",
    "@ast-grep/napi": "0.43.0",
    "@fastify/autoload": "^6.0.3",
    "@fastify/cors": "^10.0.2",
    "@fastify/helmet": "^13.0.2",
    "@fastify/rate-limit": "^11.0.0",
    "@vscode/ripgrep": "^1.15.9",
    "dependency-cruiser": "^17.4.3",
    "dotenv": "^16.4.7",
    "drizzle-orm": "^0.38.3",
    "fastify": "^5.2.0",
    "fastify-sse-v2": "^4.2.1",
    "fastify-type-provider-zod": "^4.0.2",
    "fflate": "^0.8.3",
    "graphology": "^0.26.0",
    "graphology-metrics": "^2.4.0",
    "js-tiktoken": "^1.0.21",
    "octokit": "^4.0.3",
    "openai": "^4.77.0",
    "p-queue": "^8.0.1",
    "postgres": "^3.4.5",
    "simple-git": "^3.27.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "^10.16.0",
    "@types/node": "^22.10.0",
    "drizzle-kit": "^0.30.1",
    "pino-pretty": "^13.0.0",
    "testcontainers": "^10.16.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
