import { HomeView } from "./_components/HomeView";

/* Route: / (root). Thin route entry — the redirect logic, empty state and
   styles are colocated under _components/HomeView. */
export default function HomePage() {
  return <HomeView />;
}
