export const MODAL_WIDTH = 840;

/** Agent name preferred as the default attach target when one exists. */
export const PREFERRED_AGENT_NAME = "General Reviewer";

/** Sentinel for "Don't attach yet" in the agent select. */
export const NO_AGENT = "";
