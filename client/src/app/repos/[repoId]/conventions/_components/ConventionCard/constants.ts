/** Confidence above which the meter reads as healthy. Presentation only. */
export const HIGH_CONFIDENCE = 0.85;

/** Below this the meter reads as weak. Presentation only. */
export const LOW_CONFIDENCE = 0.65;
